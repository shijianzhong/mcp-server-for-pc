import { spawn } from "child_process"; const proc = spawn("weather", [], {stdio: ["pipe", "pipe", "pipe"]}); proc.stderr.on("data", data => console.error("STDERR:", data.toString())); proc.stdout.on("data", data => console.log("STDOUT:", data.toString())); proc.stdin.write(JSON.stringify({jsonrpc: "2.0", id: 1, method: "mcp.server.listTools", params: {}}) + "\n"); setTimeout(() => proc.kill(), 5000);
